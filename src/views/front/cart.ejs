<%-include('./partials/header') %>

<body>
    <div id="main">
      <div class="container" style="padding:100px 0;">
          <div class="row">
              <div class="col-12">
                <%- include("./partials/cartInside", {cartPhones}) %>
              </div>
          </div>
          <% if(cartPhones.length){ %>
               <div class="row mt-5">
              <div class="col-12 d-flex justify-content-end">
                  <form method="POST">
                      <button type="submit" class="btn btn-success">
                        Поръчай
                        </button>
                  </form>
                  
              </div>
          </div>
         <% } %>
         
      </div>
  </div>
</body>

<%-include('./partials/footer') %>